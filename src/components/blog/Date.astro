---
import { SITE } from '@src/consts'

interface Props {
	date: Date
}

const { date } = Astro.props
const readable = new Intl.DateTimeFormat(SITE.LOCALE, {
	day: 'numeric',
	month: 'long',
	year: 'numeric',
})
---

<time datetime={date.toISOString()}>
	{readable.format(date)}
</time>
